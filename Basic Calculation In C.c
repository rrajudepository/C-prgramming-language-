#include <stdio.h>
#include<math.h>
int main() {
    
 /*float a = 22;
 float b = a;
 float c = b+1;
 float d=c,e;
 float x, y= b*c;
 int f,g,h;
 f = g = h = 5;
 float z = b+c;
 float i = a/g;
 
 float power = pow(b,2);
 printf("%f \n", power);*/
 
/* int a = 16%10;  // % does not work on float value
 int b = 20%10; //Give reminder
    int c = -8%3; // negative reminder
 printf(" reminder is : %d \n", c);
    printf("%f", 2.0/3);
  */
    
    int a = 1.999999; // Convert into integer 1 not 2;
    //Operator Precedence (Use Bodmass)
    int b = 4+9*10;
    int c = 4*3/6*2; //Calculate left to right
    printf("%d \n", c);
    int d = 5*2 - 3*2;
    int e = 5*(6/3)*3;
    int f = (5+10)*2-10;
    printf("%d \n", f);
 
 
 
 return 0;
 
 
 

}